# Vendored hook scripts

The contents of `hooks/` and `lib/` (excluding the .sh files at the parent
level) are vendored from the **Everything Claude Code (ECC)** project:

- Upstream: https://github.com/affaan-m/everything-claude-code
- License: MIT — Copyright (c) 2026 Affaan Mustafa

These scripts were copied verbatim and are not modified locally. To upgrade,
re-fetch from upstream `main` into `claude/scripts/{hooks,lib}/`.

The full MIT license text from upstream is preserved in `LICENSE-ECC` in
this same directory.
